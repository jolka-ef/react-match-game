/**
 * Checks if array includes given element.
 * @param {!Array} array - The array to check.
 * @param {!string} element - The element to look for.
 */
export const isInTheArray = (array, element) => !!(array.indexOf(element) + 1);
